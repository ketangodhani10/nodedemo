// //import express from 'express';
// const express = require('express');
// //const ctrlUser = require("../controllers/users.js");
// import { createUser, getUsers, getUser, deleteUser, patchUser, putUser } from '../controllers/users.js';

// const router = express.Router();

// router.get('/', getUsers);
// router.post('/', createUser);
// router.get('/:id', getUser);
// router.delete('/:id', deleteUser);
// router.patch('/:id', patchUser);
// router.put('/:id', putUser);


// // router.get('/', ctrlUser.getUsers);
// // router.post('/', ctrlUser.createUser);
// // router.get('/:id', ctrlUser.getUser);
// // router.delete('/:id', ctrlUser.deleteUser);
// // router.patch('/:id', ctrlUser.patchUser);
// // router.put('/:id', ctrlUser.putUser);

// export default router;
